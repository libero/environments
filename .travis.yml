sudo: false

language: minimal

before_install:
    - openssl aes-256-cbc -K $encrypted_a389905ea254_key -iv $encrypted_a389905ea254_iv -in keys/single-node--unstable.key.enc -out keys/single-node--unstable.key -d

script:
    - shellcheck scripts/*.sh

deploy:
    provider: script
    script: bash scripts/deploy.sh ec2-user@34.239.171.22 keys/single-node--unstable.key

    on:
        branch: master
